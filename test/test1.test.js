// const model = require('../models/XXXXX.model')

describe('Basic Test', () => {

  describe('Base', () => {
    test('It should...', () => {
      console.log(process.env.NODE_ENV)
      console.log(process.env.DATABASE_URL)
      expect(true).toBe(true)
    })
  })

})
